server:
  port: 8085

spring:
  application:
    name: fraud-service
  datasource:
    url: jdbc:postgresql://localhost:5432/unified_banking_fraud
    username: unibank_admin
    password: UniBank@Local123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true